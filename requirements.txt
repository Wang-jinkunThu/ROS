# ============================================================
#  Windows 编辑环境依赖（Python 3.8）
#  用途：仅用于 IDE 代码补全/语法检查，不运行 ROS
#  ROS 包 (rospy, cv_bridge 等) 无法在 Windows 安装，
#  通过 VSCode 配置忽略其 import 报错，见 .vscode/settings.json
# ============================================================
#
#  ========== 环境搭建（已安装 conda 的前提下，1 分钟） ==========
#
#  conda create -n ros_edit python=3.8
#  conda activate ros_edit
#  pip install -r requirements.txt
#
# ============================================================

# 代码中用到的 pip 可安装包
numpy==1.24.3
opencv-python==4.8.1.78

# Python 标准库（math, threading）无需安装
